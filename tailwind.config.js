/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // Enable class-based dark mode
  content: [
    "./templates/**/*.html", // Scan all HTML files in templates directory and subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
